services:
  - type: web
    name: avva-home-foods
    env: docker
    dockerfilePath: ./Dockerfile
    port: 8080
    plan: free
    memoryMB: 512
    envVars:
      - key: SPRING_DATA_MONGODB_URI
        scope: build,runtime
      - key: GOOGLE_CLIENT_ID
        scope: build,runtime
      - key: SERVER_SERVLET_CONTEXT_PATH
        value: /
    region: oregon
    autoDeployFromGitHub: true
    maxShutdownDelaySeconds: 30
